name: Welcome

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

jobs:
  welcome:
    uses: opendecree/.github/.github/workflows/welcome.yml@main
    with:
      repo_name: decree
    secrets: inherit
