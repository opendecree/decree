spec_version: v1
schema:
  name: example-app
  fields:
    app.name:
      type: string
    features.dark_mode:
      type: bool
    server.rate_limit:
      type: integer
      constraints: { minimum: 1, maximum: 10000 }
tenant:
  name: acme-corp
config:
  values:
    app.name: { value: "Acme Corp" }
    features.dark_mode: { value: true }
    server.rate_limit: { value: 1000 }
