# Asserts: validations[*].rule must be a non-empty string.
spec_version: v1
name: payments
fields:
  payments.min:
    type: integer
  payments.max:
    type: integer
validations:
  - rule: ""
    message: "rule cannot be empty"
